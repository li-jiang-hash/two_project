<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aaa.group_three.dao.OOrderMapper">

    <select id="findOrder" resultType="map">
        select o.*,b.sname,g.* from o_order o , b_business_info b,g_goods g
        where o.bid=b.id and  o.goodsid=g.id and o.uid=#{id} and o.state=#{zhuangtai}
    </select>
</mapper>
